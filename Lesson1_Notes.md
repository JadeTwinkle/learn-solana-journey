# Lesson 1: 区块链基础与 Solana 核心概念

## 1. 区块链的演变
- **Bitcoin (BTC):** 数字黄金，主要解决点对点支付。采用 UTXO 模型。
- **Ethereum (ETH):** 世界计算机，引入智能合约。采用账户模型，但顺序执行导致性能瓶颈。
- **Solana (SOL):** 高性能公链。核心创新是 **PoH (历史证明)** 和 **并行执行**，实现了高 TPS 和低费率。

## 2. Solana 核心编程模型
> 核心心法：**在 Solana 上，一切皆账户 (Everything is an Account)。**

- **Account (账户):** 存储数据的地方。类似于文件系统中的文件。
  - 需要支付 Rent (租金) 来维持存储。
- **Program (程序):** 也就是智能合约。
  - **无状态 (Stateless):** 程序本身不存数据，它只负责处理逻辑。数据存在外部传进来的 Account 里。
- **Transaction (交易):** 与区块链交互的最小单位。
  - **原子性:** 交易内的指令要么全成，要么全败。

## 3. 代币模型 (SPL Token)
- **Mint Account:** 定义代币的基本信息（供应量、精度等）。
- **Associated Token Account (ATA):** 关联代币账户。通过算法由“钱包地址+代币地址”计算得出，用来接收特定的代币。
- **NFT:** 本质上是精度为 0，供应量为 1 的 SPL Token。

## 4. 学习心得
- Solana 的设计思想是“硬件友好型”，充分利用多核 CPU 进行并行处理。
- 不需要从零写代码发币，通过 SPL Standard 可以直接配置出代币。
